
<h2> Posts from newsletters you may be interested in </h2>
<% @posts.each do |post| %> 
  <div>
    <br>
    <%= image_tag post.image.variant(resize_to_fill: [300, 300]) if post.image.attached? %>
    <br>
    <%= link_to post.description, post_path(post) %>
    <br>
    <hr>
  </div>
<% end %>
